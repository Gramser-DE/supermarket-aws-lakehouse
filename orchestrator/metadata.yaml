project_type: standalone

variables_dir: ./variables

variables_retention_period: null

emr_config:
  compute_type: local

help_improve_mage: true
notification_config:
  alert_on:
  - trigger_failure
  - trigger_passed_sla
  slack_config:
    webhook_url: "{{ env_var('MAGE_SLACK_WEBHOOK_URL') }}"
  teams_config:
    webhook_url: "{{ env_var('MAGE_TEAMS_WEBHOOK_URL') }}"
project_uuid: 7c29bef6d22941ae80518d5788cbd26b
pip_config:
  requirements_file: requirements.txt
